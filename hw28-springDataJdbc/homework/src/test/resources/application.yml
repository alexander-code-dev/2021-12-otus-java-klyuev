spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;
    username: sa
    driver-class-name: org.h2.Driver
    initialization-mode: always
    data:
      - data.sql
  main:
    banner-mode: off